-- Superseded by server/hero.js (ensureHeroSlides), which adds the per-slide CTA
-- columns and seeds the slides step by step at startup. Kept as a no-op so the
-- _migrations bookkeeping is identical on servers that already ran this file.
SELECT 1;
